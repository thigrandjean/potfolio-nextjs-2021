import styled from 'styled-components'

export const Logo = styled.header`
  position: fixed;
  top: 2rem;
  left: 2rem;
  color: var(--white);
  z-index: 1000;
  svg .logobg {
    fill: var(--color-main);
  }
`
